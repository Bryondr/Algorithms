    static long aVeryBigSum(long[] ar)
    {
        Scanner scan = new Scanner(System.in);
        long sum = 0;
        for (int i = 0; i<ar.length;i++)
        {
            sum = sum+ar[i];
        }
        return sum;
    }
